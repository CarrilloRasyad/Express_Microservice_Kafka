## INSTALL PACKAGE EXPRESS, JWT TOKEN, PGSQL, & BCRYPT
- npm install express pg bcryptjs jsonwebtoken dotenv

## RUN SERVER
- npm run dev (run local on port 9000)

## INSTALL NODEMON
- npm install nodemon --save-dev

